<Window x:Class="PowerHotkeysWPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PowerHotkeysWPF"
        mc:Ignorable="d"
        Title="PowerKeys - Keyboard Shortcuts" 
        Height="600" Width="1000"
        WindowStartupLocation="CenterScreen"
        Topmost="True"
        WindowStyle="SingleBorderWindow"
        ResizeMode="CanResize"
        Background="#0E0E0E">
    
    <Window.Resources>
        <!-- Dark Button Style -->
        <Style x:Key="DarkButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#1E1E1E"/>
            <Setter Property="Foreground" Value="#FFFFFF"/>
            <Setter Property="BorderBrush" Value="#333333"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="2">
                            <ContentPresenter HorizontalAlignment="Center" 
                                            VerticalAlignment="Center" 
                                            Content="{TemplateBinding Content}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#2D2D2D"/>
                                <Setter Property="BorderBrush" Value="#00D084"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#00D084"/>
                                <Setter Property="BorderBrush" Value="#00D084"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Dark TextBox Style -->
        <Style x:Key="DarkTextBoxStyle" TargetType="TextBox">
            <Setter Property="Background" Value="#1E1E1E"/>
            <Setter Property="Foreground" Value="#FFFFFF"/>
            <Setter Property="BorderBrush" Value="#333333"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="4"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="2">
                            <ScrollViewer x:Name="PART_ContentHost" 
                                        Margin="{TemplateBinding Padding}"
                                        VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter Property="BorderBrush" Value="#00D084"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Dark ComboBox Style -->
        <Style x:Key="DarkComboBoxStyle" TargetType="ComboBox">
            <Setter Property="Background" Value="#1E1E1E"/>
            <Setter Property="Foreground" Value="#FFFFFF"/>
            <Setter Property="BorderBrush" Value="#333333"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="4"/>
        </Style>

        <!-- Dark Slider Style -->
        <Style x:Key="DarkSliderStyle" TargetType="Slider">
            <Setter Property="Background" Value="#333333"/>
            <Setter Property="Foreground" Value="#00D084"/>
        </Style>

        <!-- Dark ScrollViewer Style -->
        <Style x:Key="DarkScrollViewerStyle" TargetType="ScrollViewer">
            <Setter Property="Background" Value="#0E0E0E"/>
        </Style>

        <Style x:Key="ShortcutCardStyle" TargetType="Border">
            <Setter Property="Background" Value="#1E1E1E"/>
            <Setter Property="BorderBrush" Value="#333333"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="4"/>
            <Setter Property="Margin" Value="4"/>
            <Setter Property="Padding" Value="8"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#2D2D2D"/>
                    <Setter Property="BorderBrush" Value="#00D084"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        
        <Style x:Key="CategoryHeaderStyle" TargetType="TextBlock">
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="Foreground" Value="#B0B0B0"/>
            <Setter Property="Margin" Value="0,0,0,2"/>
        </Style>
        
        <Style x:Key="ShortcutKeysStyle" TargetType="TextBlock">
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Foreground" Value="#00D084"/>
            <Setter Property="Margin" Value="0,2"/>
        </Style>
        
        <Style x:Key="DescriptionStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Foreground" Value="#FFFFFF"/>
            <Setter Property="TextWrapping" Value="Wrap"/>
            <Setter Property="Margin" Value="0,2,0,0"/>
        </Style>

        <Style x:Key="DarkTextBlockStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="#FFFFFF"/>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header with Search and Filters -->
        <Grid Grid.Row="0" Background="#1E1E1E" Margin="0,0,0,8">
            <Grid.Effect>
                <DropShadowEffect ShadowDepth="1" BlurRadius="3" Opacity="0.4" Color="#000000"/>
            </Grid.Effect>
            
            <Grid Margin="16,12">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Orientation="Horizontal">
                    <TextBlock Text="Search:" VerticalAlignment="Center" Margin="0,0,8,0" Style="{StaticResource DarkTextBlockStyle}"/>
                    <TextBox Name="SearchTextBox" Width="200" Height="24" 
                             Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                             VerticalContentAlignment="Center"
                             Style="{StaticResource DarkTextBoxStyle}"/>
                    
                    <TextBlock Text="Category:" VerticalAlignment="Center" Margin="16,0,8,0" Style="{StaticResource DarkTextBlockStyle}"/>
                    <ComboBox Name="CategoryComboBox" Width="150" Height="24"
                              ItemsSource="{Binding Categories}"
                              SelectedItem="{Binding SelectedCategory}"
                              Style="{StaticResource DarkComboBoxStyle}"/>
                </StackPanel>

                <StackPanel Grid.Column="1" Orientation="Horizontal" Margin="16,0">
                    <TextBlock Text="Columns:" VerticalAlignment="Center" Margin="0,0,8,0" Style="{StaticResource DarkTextBlockStyle}"/>
                    <Slider Name="ColumnsSlider" Width="100" Minimum="3" Maximum="8" 
                            Value="{Binding GridColumns}" TickFrequency="1" IsSnapToTickEnabled="True"
                            Style="{StaticResource DarkSliderStyle}"/>
                    <TextBlock Text="{Binding GridColumns}" VerticalAlignment="Center" Margin="8,0,0,0" Style="{StaticResource DarkTextBlockStyle}"/>
                </StackPanel>

                <Button Grid.Column="2" Content="Settings" Width="80" Height="24" 
                        Click="SettingsButton_Click"
                        Style="{StaticResource DarkButtonStyle}"/>
            </Grid>
        </Grid>

        <!-- Main Content Area -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" Style="{StaticResource DarkScrollViewerStyle}">
            <ItemsControl Name="ShortcutsItemsControl" 
                          ItemsSource="{Binding FilteredShortcuts}"
                          Margin="12">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <UniformGrid Columns="{Binding GridColumns}"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Style="{StaticResource ShortcutCardStyle}">
                            <StackPanel>
                                <TextBlock Text="{Binding Category}" 
                                         Style="{StaticResource CategoryHeaderStyle}"/>
                                <TextBlock Text="{Binding Keys}" 
                                         Style="{StaticResource ShortcutKeysStyle}"/>
                                <TextBlock Text="{Binding Description}" 
                                         Style="{StaticResource DescriptionStyle}"/>
                                <TextBlock Text="{Binding Application}" 
                                         FontSize="10" Foreground="#888888" 
                                         Margin="0,2,0,0"/>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <!-- Status Bar -->
        <Grid Grid.Row="2" Background="#1E1E1E" Height="24">
            <Grid.Effect>
                <DropShadowEffect ShadowDepth="-1" BlurRadius="3" Opacity="0.4" Color="#000000"/>
            </Grid.Effect>
            
            <StackPanel Orientation="Horizontal" Margin="12,0" VerticalAlignment="Center">
                <TextBlock Text="Shortcuts: " Style="{StaticResource DarkTextBlockStyle}"/>
                <TextBlock Text="{Binding FilteredShortcuts.Count}" Style="{StaticResource DarkTextBlockStyle}"/>
                <TextBlock Text=" | Press Ctrl+Alt+K to toggle visibility" Margin="20,0,0,0" Foreground="#B0B0B0"/>
            </StackPanel>
        </Grid>
    </Grid>
</Window>
